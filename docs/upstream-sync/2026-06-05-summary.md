# Sunshine Upstream Sync Summary - 2026-06-05

## Refreshed Heads

- Apollo local head: `99595263`
- Sunshine upstream head: `fa4fbbdd`
- Sunshine upstream tag description: `v2026.605.170308`
- Merge base: `1a96d135140ddfe5255516971c88f112b9da5beb`

The upstream fetch brought in `sunshine-upstream/master`. Git rejected local tag updates for `v0.3.0`, `v0.3.1`, and `v0.4.0` because this fork already has conflicting tags. The upstream branch is still usable for diffing and cherry-pick selection.

## Generated Reports

- `2026-06-05-left-right-cherry-pick.log`: focused `git log --left-right --cherry-pick --oneline` for config, encoder, stream, RTSP, NVENC, and build subsystems.
- `2026-06-05-range-diff.txt`: `git range-diff --creation-factor=80` between Apollo and Sunshine from the merge base.
- `2026-06-05-subsystem-diff-stat.txt`: focused `git diff --stat` for the same subsystem set.

## Initial Read

The focused left/right log contains 185 differing commits. Apollo-specific virtual display, OTP/API, permission, web UI, branding, and macOS build work is interleaved with upstream encoder, RTSP, capture, and packaging changes. Do not rebase or wholesale merge.

## Candidate Import Queue

Review these upstream commits first because they align with the latency/network/security plan:

- `31b85802` - `fix: redact logging of sensitive config & CSRF validation (#4955)`
- `3a196379` - `fix(web): disconnect only the disabled client instead of all sessions (#5138)`
- `3a69acef` - `feat(rtsp): add option to limit packetsize for clients that cannot configure it (#5153)`
- `07099901` - `build(deps): Add SUNSHINE_SYSTEM_VULKAN_HEADERS option (#5103)`
- `5bacfd59` - `fix(nvenc): Include bitstream restrictions in H.264/HEVC SPS (#4556)`
- `a5af7907` - `feat(nvenc): support for split frame encoding on GPUs with 2+ nvenc blocks (#4892)`
- `7e3cf702` and other dependency bumps only as needed for an encoder/capture fix.

Apply candidates in small batches and build after every batch. Preserve Apollo virtual display/session launch behavior, Apollo config names, OTP/API changes, branding, and local macOS packaging edits.
